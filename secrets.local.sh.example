#!/usr/bin/env bash
# TEMPLATE — copy to `secrets.local.sh` and fill in your key, OR (preferred) keep your real secret
# in the parent github_upload/secrets.local.sh and just `source` that before running.
# Only the `--judge openai` (GPT-4o, BYOK) backend needs this; the default local `qwen3-14b` judge
# needs NO key (just a GPU + model weights). `secrets.local.sh` is git-ignored — never commit it.

export OPENAI_API_KEY="..."   # OpenAI key, only for `python -m src.judge_clusters --judge openai`
